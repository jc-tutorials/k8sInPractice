apiVersion: v1
kind: Service
metadata:
  name: gateway
spec:
  type: ClusterIP          # stays internal - the Ingress is the way in
  selector:
    app: gateway
  ports:
    - port: 80
      targetPort: 3000
