# A patch showing the two numbers that decide what your users feel.
# Apply with:  kubectl patch deployment gateway --patch-file act3-load/rollout-strategy.yaml
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0     # never drop below the replica count
